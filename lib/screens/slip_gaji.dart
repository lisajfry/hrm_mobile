import 'package:flutter/material.dart';

class SlipGajiScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Detail Slip Gaji'),
      ),
      body: Center(
        child: Text('Detail Slip Gaji akan ditampilkan di sini'),
      ),
    );
  }
}
